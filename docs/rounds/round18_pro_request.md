# Round-18 brief — web GPT-5.5-Pro (pointer)

> **Canonical, copy-ready brief at repo root: [`BRIEF_FOR_PRO.md`](../../BRIEF_FOR_PRO.md)** (round-17
> reply + audit in `round17_pro_response.md` / `round17_audit.md`).

## What this is
Round-18 = the **single remaining R+ theorem** after round-17 collapsed the dichotomy: the **energy-to-cut
lemma (EC)** — `𝓔_{>m}(h)≥𝔇/2` (high-dim compatibility energy, `m=O(log(d/𝔇))`) ⟹ a poly-discoverable
prefix face + block `A` with `d_TV(L(|X∩A|),PB)≥(𝔇/d)^C`. (EC) ⟺ R+ on this route. Sharpest form **(MM-cut):
does minor-minimality + poly `𝔇` FORCE a poly-visible cut/NR defect?** The crux = **is cut-blindness
compatible with minor-minimality?** `PG(2,q)` (minor-minimal-flavored, growing, cut-visible `Ω(d^{−3/2})`)
says R+; the sparse-paving family (cut-blind but NOT minor-minimal — a planted 8-elt minor) says cut-blindness
needs a hidden small minor, which minor-minimality forbids. (R+) prove every minor-minimal non-HPP matroid
has a low-complexity flat with count law poly-far from PB; (R−) build a full-dimensional minor-minimal
cut-blind family. Full method freedom. **Lean: R+ ~58–62/40.**
