# Round-16 brief — web GPT-5.5-Pro (pointer)

> **Canonical, copy-ready brief at repo root: [`BRIEF_FOR_PRO.md`](../../BRIEF_FOR_PRO.md)** (round-15
> reply + audit in `round15_pro_response.md` / `round15_audit.md`).

## What this is
Round-16 = settle the **general compatibility-aware σ_min-localization (36)** — the last step of the R+
pipeline, now correctly normalized after round-15 solved `g_s` (`NR=Θ(√I_i)`, caught by an interior channel)
and corrected the certificate from determinant `[−det]/k^{k/2+1}` to **σ_min of a negative Toeplitz section**
(Lemma 1.1). Target: `I_i≥η ⟹` a poly-discoverable descendant has covariance `≥(η/d)^C` OR a bounded-channel
rank law with a negative Toeplitz section `σ_min≥(η/d)^C`. Pushes BOTH directions: (R+) localize the affinity
defect into a poly-σ_min near-singular Toeplitz window / a `g_s`-like structure theorem for minor-minimal
obstructions; (R−) the ONLY surviving hope = a **genuinely entangled** obstruction (core correlated with the
environment so no product channel + descendant exposes the violation; non-symmetric, rank-statistic-hidden).
Prove (36) ⟹ R+; build the entangled family ⟹ R−. Full method freedom. **Lean: R+ ~58–62/40.**
