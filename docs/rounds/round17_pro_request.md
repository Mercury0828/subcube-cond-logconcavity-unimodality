# Round-17 brief — web GPT-5.5-Pro (pointer)

> **Canonical, copy-ready brief at repo root: [`BRIEF_FOR_PRO.md`](../../BRIEF_FOR_PRO.md)** (round-16
> reply + two audits in `round16_pro_response.md` / `round16_audit.md`).

## What this is
Round-17 = the **decisive dichotomy** after round-16 made the foundation rigorous and reframed both sides.
**R+ ⟺ the cut-or-minor removal lemma** (`𝔇(h)≥η ⟹` a block with a two-block rank shadow `≥(η/d)^C`, OR a
poly-likely `O(log(d/η))`-dim descendant `≥(η/d)^C`-far from SR). **R− ⟺ a degenerating excluded-minor
family** (asymptotic minor-minimal non-HPP `ν_d`, every proper minor SR, `𝔇≥d^{−O(1)}`, but `NR=d^{−ω(1)}`).
**The sharpest sub-question = MEM:** must a minor-minimal non-HPP obstruction's own SR-margin be `≥poly(η,1/d)`
(⟹ minor branch fires ⟹ R+), or can it vanish while `𝔇` stays poly (⟹ R−)? The lever = the exact two-block
constraint (the obstruction must survive every two-block symmetrization). Likely the decisive round. Full
method freedom. **Lean: R+ ~55–60/40, holds.**
