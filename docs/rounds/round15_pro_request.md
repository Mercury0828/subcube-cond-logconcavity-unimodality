# Round-15 brief — web GPT-5.5-Pro (pointer)

> **Canonical, copy-ready brief at repo root: [`BRIEF_FOR_PRO.md`](../../BRIEF_FOR_PRO.md)** (round-14
> reply + audit in `round14_pro_response.md` / `round14_audit.md`).

## What this is
Round-15 = the **corrected target (35)** after round-14 refuted the root-depth route: a
**compatibility-aware total-positivity localization** — `I_i(h)≥η ⟹` a poly-discoverable descendant has a
Boolean covariance `≥(η/d)^C` OR a bounded-channel Toeplitz minor with `[−det]_+/k^{k/2+1}≥(η/d)^C` (poly
regime `k=O(log d/loglog d)`). Key structural correction carried in: localize *failure of compatibility*,
not Hellinger (spectral independence localizes the wrong quantity). **Names `g_s` (non-SR parent, every
proper Boolean conditional exactly SR) as the candidate hard core** — decisive test: is `g_s`'s
global-only obstruction caught by a bounded-channel PF minor with margin inverse-poly under the natural
lift (⟹ R+), or does it decay `2^{−Ω(d)}` (⟹ R−)? Prove (35) ⟹ R+; a spread `g_s`-family evading all
poly-discoverable descendants ⟹ R−. Full method freedom. **Lean: moderate R+ ~55–60/40.**
